import { AcademicProgramRequestModel } from './academic-program-request-model';

describe('AcademicProgramRequestModel', () => {
  it('should create an instance', () => {
    expect(new AcademicProgramRequestModel()).toBeTruthy();
  });
});
