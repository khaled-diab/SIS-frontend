import { AcademicTermRequestModel } from './academic-term-request-model';

describe('AcademicTermRequestModel', () => {
  it('should create an instance', () => {
    expect(new AcademicTermRequestModel()).toBeTruthy();
  });
});
