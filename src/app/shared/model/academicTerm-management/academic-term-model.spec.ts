import { AcademicTermModel } from './academic-term-model';

describe('AcademicTermModel', () => {
  it('should create an instance', () => {
    expect(new AcademicTermModel()).toBeTruthy();
  });
});
