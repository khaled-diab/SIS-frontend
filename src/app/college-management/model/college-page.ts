import {CollegeModel} from './college-model';

export class CollegePage {
  content: CollegeModel[];
  totalPages: number;
  totalElements: number;
  size: number;
  number: number;
  numberOfElements: number;
}
